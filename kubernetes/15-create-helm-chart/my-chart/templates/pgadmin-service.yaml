apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
  labels: 
    app: pgadmin
spec:
  ports:
    - protocol: TCP
      port: 8099
      targetPort: 80
  selector:
    app: pgadmin
  type: LoadBalancer
